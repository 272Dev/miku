services:
  - type: web
    name: miku-beam
    runtime: node
    buildCommand: cd web-client && npm install && npm run build
    startCommand: cd web-client && npm install -g serve && serve -s dist -l 3000
    envVars:
      - key: NODE_ENV
        value: production
